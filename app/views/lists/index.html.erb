<div class="screen-container">
  <div class="screen">
    <div class="container col-4">
      <h2 class="movie-credit text-center">A LIST BY NAMKHING</h1>
      <h1 class="movie-title text-center">Watch list</h1>
    </div>
     <%# <div class="category">
        <h3>TEST</h3>
      </div%>
    <%= link_to "- Create your new list here", new_list_path, class: "subtitle" %>
  </div>

</div>


  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list), class: "category" %></li>
    <% end %>
  </ul>

<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>
